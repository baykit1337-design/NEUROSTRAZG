curl_cffi>=0.7        # HTTP-клиент с TLS-отпечатком Chrome
beautifulsoup4>=4.12  # парсинг страницы главы
lxml>=5.0             # быстрый бэкенд для BeautifulSoup
flask>=3.0            # локальный веб-интерфейс
python-docx>=1.1      # вывод глав в .docx
tqdm>=4.66            # прогресс-бар в CLI (необязательно)
pymorphy3>=2.0        # морфология для проверки орфографии (необязательно)
pymorphy3-dicts-ru>=2.4  # русский словарь к нему (необязательно)
pyspellchecker>=0.8   # подсказки «чем заменить» (необязательно)
fonttools>=4.50       # расшифровка названий в рейтинге Фанкью (необязательно)
brotli>=1.1           # без него fontTools не распакует .woff2 (необязательно)
pillow>=10.0          # сверка начертаний, когда имена глифов обезличены (необязательно)
numpy>=1.24           # она же: сравнение картинок глифов (необязательно)
