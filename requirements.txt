# Границы сверху стоят не для строгости.
#
# Без них программа ломается без единого нашего коммита: вышла новая
# мажорная версия чужой библиотеки, человек поставил зависимости на новой
# машине — и получил ошибку, которой вчера не было. Верхняя граница
# поднимается вручную, после того как на новой версии всё проверено.

curl_cffi>=0.7,<0.18  # HTTP-клиент с TLS-отпечатком Chrome
beautifulsoup4>=4.12,<5  # парсинг страницы главы
lxml>=5.0,<7          # быстрый бэкенд для BeautifulSoup
flask>=3.0,<4         # локальный веб-интерфейс
python-docx>=1.1,<2   # вывод глав в .docx
tqdm>=4.66,<5         # прогресс-бар в CLI (необязательно)
pymorphy3>=2.0,<3     # морфология для проверки орфографии (необязательно)
pymorphy3-dicts-ru>=2.4,<3  # русский словарь к нему (необязательно)
pyspellchecker>=0.8,<1  # подсказки «чем заменить» (необязательно)
fonttools>=4.50,<5    # расшифровка названий в рейтинге Фанкью (необязательно)
brotli>=1.1,<2        # без него fontTools не распакует .woff2 (необязательно)
pillow>=10.0,<13      # сверка начертаний, когда имена глифов обезличены (необязательно)
numpy>=1.24,<3        # она же: сравнение картинок глифов (необязательно)
