"""Операции — то, что делают вкладки.

Каждая операция сообщает о прогрессе одинаково: колбэк
`(сделано, всего, текст)` и флаг отмены, который проверяется перед каждым
шагом. Ошибки не глотаются: операция возвращает отчёт со списком по файлам.
"""
